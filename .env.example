# If you get CORS errors on Railway, ensure the sheet is published via:
# File → Share → Publish to web → Entire document → CSV format
# The export URL format must be:
# https://docs.google.com/spreadsheets/d/e/YOUR_TOKEN/pub?output=csv
VITE_SHEET_URL=your_google_sheet_csv_url_here